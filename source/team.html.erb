
<h1 class="text-center">My Team</h1>
<ul class="list-inline text-center padded">
  <% ["hugo", "consti"].each do |member| %>
    <li>
      <%= image_tag "#{member}.jpg", class: "img-circle avatar-extra-large" %>
      <h3><%= member.capitalize %></h3>
    </li>
  <% end %>
</ul>
